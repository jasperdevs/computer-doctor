interface:
  display_name: "Devtools Audit"
  short_description: "Review shells, PATH, runtimes, SDKs, package managers, Git tooling, and setup conflicts"
  default_prompt: "Use $devtools-audit to inspect shells, PATH, runtimes, SDKs, package managers, Git tooling, and developer-tooling conflicts on this machine."
